<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="5dp"
    app:cardBackgroundColor="@color/colorPrimary"
    app:cardCornerRadius="10dp"
    app:cardElevation="15dp"
    android:background="@color/colorPrimary">
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="10dp"
    android:descendantFocusability="blocksDescendants"
    >

    <ImageView
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:src="@drawable/default_image"
        android:layout_marginBottom="5dp"
        android:layout_marginRight="5dp"
        android:id="@+id/imageViewDelete"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="nom moto"
        android:textSize="20dp"
        android:id="@+id/motoNameDeleteListItem"
        android:layout_below="@+id/dateDeleteListItem"
        android:layout_toRightOf="@+id/imageViewDelete"
        android:layout_toEndOf="@+id/imageViewDelete"
        android:textColor="@color/colorText"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/imageViewDelete"
        android:layout_marginLeft="5dp"
        android:layout_marginBottom="5dp"
        android:layout_toLeftOf="@+id/checkboxDeleteListItem"
        android:text="date"
        android:textSize="16dp"
        android:id="@+id/dateDeleteListItem"
        android:textColor="@color/colorText"/>

    <CheckBox
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_marginLeft="5dp"
        android:layout_marginBottom="5dp"
        android:layout_alignParentRight="true"
        android:id="@+id/checkboxDeleteListItem" />

</RelativeLayout>
</android.support.v7.widget.CardView>